- [x] Update `app/waitlist/page.tsx` to add `"use client"`, React state for loading + form
- [x] Add `handleSubmit` that POSTs to `/api/waitlist` and resets form on success
- [x] Wire `form` fields to all inputs/select/textarea via `value` + `onChange`
- [x] Update submit button to show `Submitting...` when `loading` and disable during submit
- [x] Run typecheck/lint/build to verify correctness



